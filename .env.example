VITE_API_BASE_URL=https://api.example.com
VITE_BACKEND_TARGET=http://127.0.0.1
VITE_BACKEND_STREAM_TARGET=http://127.0.0.1:8001
# Local dev only: second backend instance for the SSE notification stream
# (php artisan serve is single-threaded; omit in production).
VITE_STREAM_BASE_URL=http://127.0.0.1:8001
